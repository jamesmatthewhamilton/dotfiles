# bashrc_tmp.sh - System-specific temporary configuration
#
# This file contains machine-specific bash settings that should NOT be
# synchronized across systems. It is committed once with the initial
# template and thereafter ignored by version control.
#
# Add your local customizations here (e.g., local paths, machine-specific
# aliases, environment variables for this system only).

